/* eslint-disable react/prop-types */
const Auth = ({ children }) => {
    return (
        <>
            <div className="container">
                {children}
            </div>
        </>
    );
}

export default Auth;